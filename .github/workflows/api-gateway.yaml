name: Api Gateway CI

on:
  push:
    branches: [ main ]
    paths:
      - 'api-gateway/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'api-gateway/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        working-directory: api-gateway
        run: mvn clean package -DskipTests

      - name: Build Docker image
        working-directory: api-gateway
        run: docker build -t payflow-api-gateway:${{ github.sha }} .